# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Credentials & local config — never commit
*.json.key
*credentials*
*.pem
kubeconfig*
.env
# Tokens / keys (a live slurm.jwt REST token slipped into PR #38 — never again)
*.jwt
*.token
*.key

# Go
bin/
vendor/
*.test
coverage.out
e2e-slurm-diagnostics/

# Scratch
tmp/
gemini.md

# Demo console — runtime config generated by serve.sh (local ports/URLs).
# Committed reference lives in config.example.js.
tools/demo-console/config.js
