# Code owners are requested for review automatically on matching PRs.
# GitHub handles only, no personal email. See MAINTAINERS.md.
#
# NOTE: CODEOWNERS only *requests* review; it is enforced as a merge
# requirement solely by a branch-protection rule ("Require review from Code
# Owners"). With a single maintainer that rule would block every PR, so it is
# deliberately not enabled — this file documents ownership and drives review
# requests. Revisit when a second maintainer joins (MAINTAINERS.md).

# Default owner for everything.
*                       @mrozacki

# Controller source.
/cmd/                   @mrozacki
/internal/              @mrozacki
/api/                   @mrozacki

# Deployment assets (chart, CRD) — security-sensitive.
/deploy/                @mrozacki

# Security policy and threat model changes need maintainer review.
/SECURITY.md            @mrozacki
/docs/reference/threat-model.md  @mrozacki
