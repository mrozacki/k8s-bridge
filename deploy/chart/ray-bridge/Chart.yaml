apiVersion: v2
name: ray-bridge
description: "EXPERIMENTAL — Kueue-centric admission for KubeRay RayCluster inner workloads (sibling of k8s-bridge). Validated at small scale on kind only, never on a real multi-node cluster."
type: application
version: 0.3.1
appVersion: "v0.3.1"
# Same floor as the k8s-bridge chart, for the same reason (Kueue
# Topology-Aware Scheduling / JobSet minimums) — not independently verified
# against a live cluster below 1.29; ratify or lower once checked.
kubeVersion: ">= 1.29.0-0"
